# This workflow has been replaced by build-and-push.yml
# The mirror step is now included as a job in build-and-push.yml
# This file is kept for reference only and can be safely deleted.

name: Mirror to GitHub (DEPRECATED)
on:
  # Disabled - use build-and-push.yml instead
  workflow_dispatch:

jobs:
  notice:
    runs-on: [self-hosted, rocm]
    steps:
      - name: Notice
        run: echo "This workflow is deprecated. Use build-and-push.yml instead."
